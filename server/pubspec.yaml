name: clash_royale_server
description: Backend proxy server for Clash Royale Flutter app
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.1

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.2.0
  dotenv: ^4.2.0

dev_dependencies:
  lints: ^2.1.0
